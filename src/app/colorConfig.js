import { createTheme } from '@mui/material/styles';

const colors = createTheme({
  palette: {
    primary: {
      main: '#8c9eff',
    },
    secondary: {
      main: '#f44336',
    },
  },
});

export default colors;